import { defineConfig } from 'vitest/config';

// Die Umgebung steht pro Datei im Kopf (`@vitest-environment jsdom`): Die Regeln
// laufen ohne DOM, nur das Formular braucht einen.
export default defineConfig({});
